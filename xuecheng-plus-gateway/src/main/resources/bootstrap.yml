#微服务配置
spring:
  application:
    name: gateway    #服务名   content-api-dev.yml
  cloud:
    nacos:
      server-addr: 192.168.101.65:8848
      discovery:  #服务注册相关配置
        namespace: dev402
        group: xuecheng-plus-project
      config: #配置文件相关信息
        namespace: dev402
        group: xuecheng-plus-project
        file-extension: yaml
        refresh-enabled: true
        shared-configs:
          - data-id: loggin-${spring.profiles.active}.yaml
            group: xuecheng-plus-common
            refresh: true

  profiles:
    active: dev #环境名



